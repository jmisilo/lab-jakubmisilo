import { AIWidget } from "./_components/ai-widget";

const AIWidgetPage = () => {
  return (
    <div className="fixed bottom-48 inset-x-0 w-full flex justify-center max-w-full px-5">
      <AIWidget />
    </div>
  );
};

export default AIWidgetPage;
